language: generic 
sudo: required
services:
    - docker 

before_install:
    - docker build -t vladhondru25/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true vladhondru25/docker-react npm run test